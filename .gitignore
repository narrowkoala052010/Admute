# ── PYTHON ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
find_variants.py
reconcile.py
golden_hash.py

# ── ENVIRONMENTS ──────────────────────────────────────────
venvmute/
venv/
env/
.env
.venv/

# ── ADMUTE DATA (THE PRIVATE VAULT) ───────────────────────
# We publish the logic, but we KEEP the data. 
# This is your competitive moat.
*.db
*.db-shm
*.db-wal
admute.db
recordings/
fingerprints/
logs/
*.wav

# ── CONFIGURATION ─────────────────────────────────────────
# We ignore the actual config but usually keep a template.
config.toml
# Note: You should create a 'config.toml.example' that IS tracked.

# ── IDE & OS ──────────────────────────────────────────────
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db
*.swp
*.swo

# ── SCRIPTS ───────────────────────────────────────────────
# REMOVED: find_variants.py, reconcile.py, golden_hash.py
# These MUST be tracked to establish Prior Art on GitHub.